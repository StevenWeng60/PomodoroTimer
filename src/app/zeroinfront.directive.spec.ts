import { ZeroinfrontDirective } from './zeroinfront.directive';

describe('ZeroinfrontDirective', () => {
  it('should create an instance', () => {
    const directive = new ZeroinfrontDirective();
    expect(directive).toBeTruthy();
  });
});
